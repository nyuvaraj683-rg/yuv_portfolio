# Yuvi Portfolio Backend

## Setup

1. Install dependencies:
   npm install
2. Create a .env file from .env.example and update the values.
3. Start the development server:
   npm run dev

## API Overview

- POST /api/contact
- GET /api/projects
- POST /api/auth/login
- POST /api/auth/register
- Protected admin endpoints under /api/admin
